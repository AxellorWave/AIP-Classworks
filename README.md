# 📝 Классные работы по курсу «Алгоритмизация и программирование»

Данный репозиторий содержит программные задачи, выполненные на занятиях по курсу  
**«Алгоритмизация и программирование (AIP)»**.  
Каждая работа реализована на языке **C++ (C++11+)** без использования STL-структур, чтобы отрабатывать низкоуровневые навыки.

---

## 📂 Содержание задач

Ниже приведены подробные алгоритмические описания каждой задачи вне зависимости от папки/даты.

---

### 🔢 `multiply(a, b)` *(AIP-18-10-2025/task1.cpp)*  
**Тема:** реализация умножения без оператора `*`.

**Алгоритм:**
- Функция получает два числа `a` и `b`.
- Результат `c` инициализируется нулём.
- Пока `b > 0`, к результату прибавляется `a`, `b` уменьшается на 1.
- Возвращается итоговая сумма.

📌 **Навык:** симуляция арифметических операций вручную, понимание роли циклов.

---

### `double_factorial(a)` *(AIP-18-10-2025/task1.cpp)*  
**Тема:** нахождение двойного факториала (`n!!`) с учётом чётности.

**Алгоритм:**
- Определяется старт (для чётных — `2`, для нечётных — `1`).
- Перемножаются только числа одинаковой чётности ≤ `a`.
- Умножение реализовано вручную через функцию `multiply`.

📌 **Навыки:** циклы, чётность, разложение задачи на функции.

---

### 🧮 `main.cpp` *(AIP-25-10-2025)*  
**Тема:** вычисление корней квадратного уравнения.

**Алгоритм:**
- Находит *дискриминант* вручную.
- При `D > 0` вычисляет два корня, при `D = 0` — один, при `D < 0` не выводит корней.
- Без `cmath`: квадратный корень вычисляется последовательным подбором.

📌 **Навыки:** обработка случаев, реализация математических операций вручную.

---

### 📊 `task1.cpp` *(AIP-07-11-2025)*  
**Тема:** суммирование элементов массива.

**Алгоритм:**
- Ввод фиксированного набора значений.
- Подсчитывается суммарное значение через цикл.
- Использует статический массив и индексацию.

📌 **Навык:** работа с массивами на уровне указателей и индексов.

---

### 🔍 `task2.cpp` *(AIP-07-11-2025)*  
**Тема:** поиск минимума/максимума.

**Алгоритм:**
- Инициализация `min`, `max` из первого элемента.
- Проход по массиву, сравнение и замена при необходимости.

📌 **Навык:** поиск в одном проходе, линейные алгоритмы.

---

### 📐 `task3.cpp` *(AIP-07-11-2025)*  
**Тема:** разворот массива.

**Алгоритм:**
- Выполняется обмен элементов симметрично `i <-> n - 1 - i`.

📌 **Навык:** работа с индексами, инвариант цикла.

---

### 🔁 `task5.cpp` *(AIP-07-11-2025)*  
**Тема:** циклический сдвиг массива вправо.

**Алгоритм:**
- Сохраняется последний элемент.
- Все элементы сдвигаются вправо.
- Последний становится первым.

📌 **Навык:** внимательная работа с границами.

---

### 🔄 `task6.cpp` *(AIP-07-11-2025)*  
**Тема:** циклический сдвиг массива влево.

---

### 🧱 `task1.cpp` *(AIP-08-11-2025)*  
**Тема:** динамическое выделение массива.

**Алгоритм:**
- Массив создаётся через `new[]`
- Заполняется значениями по индексу.
- Пользователь сам задаёт размер.

📌 **Навык:** `new`, отличие от статических массивов.

---

### 🧱⚙ `task1v2.cpp` *(AIP-08-11-2025)*  
**Тема:** заполнение массива по формуле.

**Особенность:** значения массива вычисляются через функцию, а не вводятся.

---

### 📏 `task2.cpp` *(AIP-08-11-2025)*  
**Тема:** поиск индекса элемента по условию.

---

### 📎 `task3.cpp` *(AIP-08-11-2025)*  
**Тема:** копирование массива.

**Алгоритм:**
- Создаётся новый массив такого же размера.
- Элементы копируются по индексу.

📌 **Навык:** владение памятью, передача массивов по указателю.

---

### 📌 `task5.cpp` *(AIP-08-11-2025)*  
**Тема:** объединение массивов.

**Описание:** новый массив содержит элементы из двух других.

📌 **Навык:** вычисление результирующего размера, ручное копирование.

---

### 🔠 `task2.cpp` *(AIP-10-11-2025)*  
**Тема:** работа со строками `char[]`.

---

### 📉 `task3.cpp` *(AIP-10-11-2025)*  
**Тема:** подсчёт отдельных символов.

---

### 🆎 `task4.cpp` *(AIP-10-11-2025)*  
**Тема:** сравнение строк.

---

### 🔢 `task1.cpp` *(AIP-15-11-2025)*  
**Тема:** рекурсивное вычисление суммы цифр.

---

### 🔢✖ `task2.cpp` *(AIP-15-11-2025)*  
**Тема:** рекурсивное произведение цифр.

---

### 🌀 `task3.cpp` *(AIP-15-11-2025)*  
**Тема:** реверс числа через рекурсию.

---

### 📍 `task1.cpp` *(AIP-17-11-2025)*  
**Тема:** выделение и очистка динамической памяти. Начало работы с классами. Создание класса Ints.

---

## 🧠 Навыки, которые тренирует репозиторий

| Направление | Практика |
|-------------|----------|
| Арифметика без встроенных операторов | умножения, корни, факториалы |
| Работа с массивами | поиск, копирование, сдвиги, объединение |
| Динамическая память | `new[]`, указатели |
| Алгоритмы | линейные, рекурсивные, симметричные |
| Строки | поиск, счёт, сравнение |
| Рекурсия | обработки чисел, перебор |

---

## 💻 Язык и стандарт

- **C++**
- **C++11+**
- Без STL (преднамеренно)

---

### 💬 Дополнительно
Если нужно, могу:
🔧 добавить замечания об утечках памяти  
🧠 сделать диаграммы алгоритмов  
📌 предложить улучшенные версии решений
